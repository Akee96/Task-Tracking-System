apiVersion: v1
kind: Service
metadata:
    name: "initialaks"
    labels:
        app: "initialaks"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    selector:
        app: "initialaks"